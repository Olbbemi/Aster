# Aster 작업 규칙

## 적용 범위

이 파일은 Aster 저장소에서 스킬, 훅, MCP, 에이전트 실행 보조 구조와 플러그인을 개발하는 작업에 적용한다.
전역 환경과 전역 구성 요소는 Aster의 관리 범위에서 제외한다.

## 규칙의 역할

이 파일은 Aster 작업에 적용하는 운영 규칙을 정의한다.

- 공유 데이터를 확인하거나 기록해야 하는 조건
- 작업 시작, 완료 및 보류 시 수행할 행동
- 중간 산출물을 저장하고 확정된 내용을 저장소에 반영하는 절차
- 파일 배치와 Git 변경 반영 시 지켜야 할 기준
- 교차 검증을 수행해야 하는 조건

공유 데이터의 디렉토리 구조, 기록 형식과 관리 규칙은 `~/.local/share/aster/README.md`에서 안내하는 상세 규격을 따른다.
기록의 작성 및 수정 권한도 해당 규격에서 확인하며, 이 파일에 반복해서 정의하지 않는다.

## 설계와 기록 원칙

- SSOT (Single Source of Truth): 같은 규칙이나 사실의 정본은 한 곳에서 관리한다. 다른 문서에서는 정본을 참조하며, 같은 내용을 독립적으로 정의하지 않는다.
- SDD (Specification-Driven Development): 구현 전에 목적, 동작과 검증 기준을 명세한다. 구현과 검증은 해당 명세를 기준으로 수행하며, 명세를 변경해야 하면 명세를 먼저 갱신한 뒤 구현과 검증에 반영한다.

Aster의 규격과 절차를 작성하거나 작업을 수행할 때는 이 원칙을 따른다.

사용자와 설계를 논의하는 작업은 주제별로 검토하고, 합의한 범위부터 구현한다.
검토 중인 초안이나 합의되지 않은 제안을 확정된 규칙으로 적용하지 않는다.

## 작업 시작

Aster 작업을 시작할 때 `~/.local/share/aster/README.md`를 먼저 읽는다.
현재 작업에 추가로 필요한 공유 데이터와 명세는 `README.md`의 안내에 따라 확인한다.

기존 작업을 재개할 때는 관련 작업 기록과 명세에서 수행 결과, 남은 작업과 참조 자료를 확인한다.
기록을 현재 파일과 필요한 실제 근거에 대조하고, 이어서 수행할 작업을 정한다.

## 산출물 배치

설계 초안, 작업용 명세, 비교안, 검토 전 기록, 시험 결과와 그 밖의 중간 산출물은 Aster 저장소에 만들지 않고 공유 작업 데이터 경로 `~/.local/share/aster/`에 저장한다.
자료의 용도에 맞는 저장 위치는 해당 경로의 `README.md`에서 안내하는 상세 규격을 확인해 정한다.

Aster 저장소에는 완성 후에도 유지할 소스, 실행 자료, 규격, 검사와 저장소 운영 파일만 둔다.
이러한 최종 산출물은 구현 과정에서 저장소의 해당 위치에 직접 작성하거나 수정할 수 있다.
이 작업은 공유 작업 데이터에 보관할 중간 산출물을 만드는 것과 구분한다.

공유 작업 데이터에서 검토한 내용을 확정하면, 저장소에서 유지할 파일에 반영한다.
반영한 내용이 합의한 결과와 일치하는지 확인한 뒤, 역할이 끝난 초안과 중복 자료를 정리하고 해당 자료를 가리키던 참조도 함께 갱신한다.

## 파일과 경로

파일과 디렉토리는 실제 사용할 목적과 배치 위치가 정해졌을 때 생성한다.
예상 구조를 보여주기 위한 빈 디렉토리나 자리 표시용 파일은 만들지 않는다.

심볼릭 링크는 기본적인 파일 배치 방식으로 사용하지 않는다.
실제 파일이나 명시적인 참조로 해결하기 어려운 경우에만 사용한다.

심볼릭 링크가 필요하면 링크 대상, 사용 이유, 갱신 방식과 링크가 끊어졌을 때의 영향을 사용자에게 설명하고 명시적인 승인을 받은 뒤 생성한다.

## 저장소 배치

저장소에 파일이나 디렉토리를 추가하거나 배치를 변경할 때는 `standards/repository-layout.md`에서 해당 영역의 역할, 배치와 생성 조건을 확인한다.

## 개발 규격과 검사

검사를 작성하거나 수행할 때는 적용할 개발 규격과 판정 기준을 먼저 확인한다.
검사 코드는 해당 규격을 구현하며, 규격에 없는 요구사항을 검사 코드만으로 강제하지 않는다.
기계적으로 판정할 항목과 사람 또는 AI 에이전트의 판단이 필요한 항목을 구분한다.

## 시험용 구성 요소

구조나 검사 방식의 실현 가능성을 사전에 확인해야 하면, 필요한 범위의 시험용 구성 요소로 검증한다.
시험용 구성 요소를 실제 운영에 사용하려면 해당 용도의 요건과 검증 기준을 충족하는지 별도로 확인한다.

## 플러그인 배포

플러그인을 배포하기 전에 설치와 실행에 필요한 파일이 모두 배포 범위에 포함되는지 확인한다.
개발 과정에서만 사용하는 문서와 검사 자료가 배포 범위에 포함되지 않는지 확인한다.

## Git 변경 반영

커밋 요청을 받으면 작업 트리의 변경 내용을 확인하고, 변경된 파일 중 커밋에 포함할 파일과 제외할 파일을 구분해 사용자에게 제시한다.
사용자가 커밋 대상을 승인한 뒤에만 승인된 파일을 스테이징하고 커밋한다.

커밋 대상에는 현재 요청 범위에서 완료된 변경만 포함한다.
"산출물 배치"에서 정한 중간 산출물과 다른 작업의 변경은 커밋 대상에서 제외하고, 제외한 항목을 사용자에게 알린다.

커밋 전에 실제 스테이징된 파일과 변경 내용이 사용자가 승인한 범위와 일치하는지 확인한다.
승인 후 커밋 대상 파일이나 변경 내용이 승인 범위를 벗어나면, 달라진 대상을 사용자에게 다시 제시하고 승인받는다.

커밋 메시지는 영어로 작성한다. 제목은 변경 결과를 요약하고, 본문에는 변경 이유와 주요 변경 내용을 기록한다.

푸시는 사용자가 명시적으로 요청한 경우에만 수행한다.
커밋과 푸시를 함께 요청했더라도 커밋 대상을 승인받은 뒤 커밋과 푸시를 순서대로 수행한다.

기존 커밋의 수정, 이력 재작성, 강제 푸시, 변경 사항 되돌리기와 브랜치 삭제는 사용자가 해당 동작을 명시적으로 요청한 경우에만 수행한다.

## 교차 검증

교차 검증은 모든 작업에 필수로 요구하지 않는다.
수행할 수 없거나 필요하지 않다고 판단되면 생략할 수 있다.
다만 해당 작업에 적용되는 규격에 별도 조건이 있으면 그 조건을 따른다.

교차 검증을 수행할 때는 Codex 산출물을 Claude가, Claude 산출물을 Codex가 독립적으로 검증한다.

교차 검증 과정에서는 다음 내용을 기록한다.

- 요청: 무엇을 어떤 기준으로 검증해 달라고 전달했는지
- 검토 결과: 검증자가 확인한 결과와 근거
- 후속 판단: 필요한 경우, 검증 지적을 수용하거나 반박한 이유와 재검증 여부

각 기록의 저장 위치와 사용할 템플릿은 `~/.local/share/aster/README.md`에서 안내하는 `references/cargo.md`를 확인한다.

## Codex와 Claude 호환성

두 하네스에서 사용할 수 있는 공용 구성을 우선한다.
실제 비호환 사항이 확인된 경우에만 하네스 전용 파일이나 경로를 추가한다.
한 하네스에서 확인한 파일 형식, 설정과 실행 방식이 다른 하네스에서도 동일하게 지원된다고 가정하지 않는다.
